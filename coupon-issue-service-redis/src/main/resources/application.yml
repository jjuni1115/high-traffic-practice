spring:

  data:
    redis:
      host: localhost
      port: 6379

  application:
    name: coupon-issue-service-redis

  datasource:
    username: root
    password: 1115
    url: jdbc:mariadb://localhost:3306/coupon?useSSL=false&serverTimezone=Asia/Seoul
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    generate-ddl: off